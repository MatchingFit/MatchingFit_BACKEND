# FastAPI 및 서버 실행 관련
fastapi==0.110.0
uvicorn[standard]==0.29.0

# 환경 변수 로드
python-dotenv==1.0.1

# AWS S3 Presigned URL 처리
boto3==1.34.94

# KoBERT 임베딩 관련
transformers==4.30.2
torch==1.13.1
kobert-transformers==0.5.1   # 공식 지원되는 버전
sentencepiece==0.1.99        # 토크나이저 의존성

# 문서 파일 텍스트 추출
pdfminer.six==20221105       # PDF 텍스트 추출
python-docx==1.1.0           # DOCX 텍스트 추출
pdfplumber==0.10.2

# PostgreSQL DB 연결
psycopg2-binary

# FastAPI 파일 업로드 (multipart/form-data) 지원
python-multipart